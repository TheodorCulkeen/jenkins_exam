apiVersion: v1
kind: Service
metadata:
  name: nginx  
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: {{ .Values.nginx.service.port }}
      targetPort: {{ .Values.nginx.service.port }}
      nodePort: {{ .Values.nginx.nodePort }}
      
