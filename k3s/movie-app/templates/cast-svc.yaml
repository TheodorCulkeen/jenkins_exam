apiVersion: apps/v1
apiVersion: v1
kind: Service
metadata:
  name: cast-service
spec:
  selector:
    app: cast-service
  ports:
    - port: {{ .Values.cast.service.port }}
      targetPort: {{ .Values.cast.service.targetPort }}
      
